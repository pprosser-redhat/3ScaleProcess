camel:
  springboot:
    # The Camel context name
    name: bpm-config

# Binding health checks to a different port
management:
  port: 8081

# disable all management enpoints except health
endpoints:
  enabled: true
  health:
    enabled: true

#  hostname: rhcs-bpms-install-demo-accountapprovals.192.168.99.100.nip.io
bpm:
  hostname: localhost
  port: 8081
  username: bpmsAdmin
  password: test
  
